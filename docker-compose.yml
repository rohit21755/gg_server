version: "3.9"

services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: yourapp
    ports:
      - "5432:5432"

  app:
    build: .
    ports:
      - "8080:8080"
    env_file: .env
    depends_on:
      - db
